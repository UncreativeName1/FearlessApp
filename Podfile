# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fearless (iOS)' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!
    
    pod 'Introspect', '~> 0.1'

end

target 'Fearless (macOS)' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!
    
    # Pods for Fearless (macOS)
    
end
